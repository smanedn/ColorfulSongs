using UnityEngine;

public class RandomRoomGenerator : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    [Header("-----Enigmi-----")]
    [SerializeField] private MonoBehaviour parkourScript;
    [SerializeField] private MonoBehaviour wallPedana;

    private GameObject[] Parkours;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
